interface:
  display_name: "Plain Technical Prose"
  short_description: "Translate dense engineering text clearly"
  default_prompt: "Use $write-timeless-technical-prose to state the risk, mechanical fix, and guaranteed state in plain English, then explain the key concepts."

policy:
  allow_implicit_invocation: true
